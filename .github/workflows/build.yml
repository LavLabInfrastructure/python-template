name: Build Wheel

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build-wheel:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Build Docker image
      run: docker build --target dev -t myapp:dev .
    - name: Build wheel with build module
      run: docker run --rm -v "${{ github.workspace }}:/app" myapp:dev python -m build --wheel
    - name: Copy wheel to host
      run: docker cp $(docker create --rm myapp:dev):/app/dist ./
    - name: Upload wheel
      uses: actions/upload-artifact@v3
      with:
        name: wheel
        path: dist/*.whl
